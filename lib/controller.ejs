<% if (strict) { -%>
    "use strict";
<% } -%>
    
<% if (view) { -%>
module.exports = {
    async foo(req, res, next) {
        // You use this area for setting res, req data.
        const helloData = req.query.hello;  // this mean ?hello=[data]

        // include service
        const { myservice } = require('../services/indexService');
        const result = await myservice(helloData);    // default : conative
        res.send(`it say: ${result}`);
    },
    bar(req, res, next) {
        // And you can choose whether or not use service.
        // Just use controller.
        res.send("hello world!");
    }
};

/**
* [Tip] It just another way.
* 
* 
* module.exports = (req, res, next) => {
*     console.log("hello~");
*     res.send("hello world!");
* };
* 
* or
* 
* const index = {
*     inputValue : "hola",
*     foo(req, res, next) {
*         console.log("hello!");
*         res.send("hello!");
*     }, ...
* }
* module.exports = index;
* 
* etc.
*/
<% } -%>