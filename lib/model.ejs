<% if (view) { -%>
const db = require("../config/dbConnection");

module.exports = {
    testConnect() {
        return new Promise((resolve, reject) => {
            db.query('select * from a', (err, data) => {
                if(err) {
                    console.log("Query excute failed.");
                    reject(data);
                }
                console.log("Query excute success.");
                resolve(data);
            });
        });
    }
}
<% } -%>