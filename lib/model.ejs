<% if (strict) { -%>
    "use strict";
<% } -%>

<% if (view) { -%>
const db = require("../config/dbConnection");

module.exports = {
    testConnect() {
        return new Promise((resolve, reject) => {
            // myTable is your table's name.
            db.query('select * from myTable', (err, data) => {
                if(err) {
                    console.log("Query excute failed.");
                    reject(data);
                }
                console.log("Query excute success.");
                resolve(data);
            });
        });
    }
}
<% } -%>