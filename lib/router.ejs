<% if (strict) { -%>
    "use strict";
<% } -%>

<% if (view) { -%>
const express = require('express');
const router = express.Router();

// Rendering : localhost:8080/
router.get('/', function(req, res, next) {
    res.render('index', {test: "hello conative!"});
});

// Use API (Use controller) : localhost:8080/api
const { foo, bar } = require('../controllers/indexController');
router.get('/api', foo);
router.get('/api2', bar);

// Use API (Not use controller)
// router.get('/', function(req, res, next) {
//     res.send('hello conative!');
// });

/* use MiddleWare */
// router.all('*', function(req, res, next) {
//     //something here..
//     next();
// });

module.exports = router;
<% } -%>