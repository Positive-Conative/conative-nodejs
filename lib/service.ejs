<% if (view) { -%>
'use strict';

module.exports = {
    async myservice(myText="conative") {
        let result = myText;
        
        // If you want to use MYSQL... 
        
        // const { testConnect } = require('../models/testDAO');
        // result = await testConnect();
        // console.log("result:", result);

        return result;
    }
};
<% } -%>